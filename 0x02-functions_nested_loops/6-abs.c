#include "main.h"
/**
 * _abs - computes absolute value of an interger
 * @t: The interger to be computed
 * Return: the absolute value
 */
int_abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
